import { AccessPermissionsGuard } from './access-permissions.guard';

describe('AccessPermissionsGuard', () => {
  it('should be defined', () => {
    expect(new AccessPermissionsGuard()).toBeDefined();
  });
});
